# Redéploiement production — article blog 7 points de contrôle RH.
# Publication cible : 15 septembre 2026 à 07:00 (Europe/Paris) = 05:00 UTC.
name: Déploiement blog 7 points de contrôle sept. 2026

on:
  schedule:
    - cron: '0 5 15 9 *'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  redeploy-production:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Déclencher le build Vercel (commit vide)
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git commit --allow-empty -m "chore: déploiement production programmé — article blog 7 points de contrôle (15 sept. 2026)"
          git push

      - name: Vérification SEO post-déploiement
        run: node scripts/post-deploy-seo.mjs
        env:
          SITE_URL: https://www.atipikrh.com
